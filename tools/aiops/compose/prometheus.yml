# ZAYNOR AIOps demo: Prometheus config.
# Application metrics arrive via the OTel Collector's remote write
# (single ingestion plane); Prometheus also self-scrapes the collector.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8888"]
